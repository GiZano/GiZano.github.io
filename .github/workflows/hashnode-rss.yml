name: Update latest Hashnode article

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  hashnode_job:
    runs-on: ubuntu-latest
    name: Inline the latest Hashnode article into index.html

    steps:
      - name: Checkout the repo
        uses: actions/checkout@v4

      - name: Fetch and patch index.html
        run: python3 Script/update-hashnode.py

      - name: Commit and push if changed
        run: |
          if [[ `git status --porcelain index.html` ]]; then
            git config --global user.name 'GitHub Actions'
            git config --global user.email 'actions@github.com'
            git add index.html
            git commit -m "Automated hashnode article update"
            git push
          fi